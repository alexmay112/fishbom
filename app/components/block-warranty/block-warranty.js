$(document).ready(function () {
  $('.photo-slider').slick({
    // autoplay: true,
    autoplaySpeed: 1000,
    slidesToShow: 7,
    slidesToScroll: 1,
    speed: 2000,
    // centerMode: true,
    // pauseOnFocus: false,
    // focusOnSelect: true
  });
});